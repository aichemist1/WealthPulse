# Copy to scripts/aws_deploy_ssm.env (gitignored) and fill in values.
#
# AWS SSM-based deploy (no inbound SSH required).
# Requires the EC2 instance to be managed by SSM:
# - SSM Agent installed (common on Amazon Linux / Ubuntu images)
# - Instance IAM role includes AmazonSSMManagedInstanceCore
# - Instance has outbound access to SSM endpoints (internet or VPC endpoints)

# Required:
AWS_PROFILE=skprofile
AWS_REGION=us-east-1
WEALTHPULSE_AWS_INSTANCE_ID=i-xxxxxxxxxxxxxxxxx

# Repo to pull on the instance (recommended).
# Ensure the repo contains the latest code you want deployed.
WEALTHPULSE_REPO_URL=https://github.com/aichemist1/WealthPulse.git
WEALTHPULSE_REPO_BRANCH=main

# Remote install directory:
WEALTHPULSE_REMOTE_DIR=/opt/wealthpulse

# Local env file to upload (recommended):
WEALTHPULSE_LOCAL_PROD_ENV=./prod.env

# Optional:
# Set to "true" to install docker on the instance (Ubuntu). Default: true.
WEALTHPULSE_INSTALL_DOCKER=true

