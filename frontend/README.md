# WealthPulse Admin UI (v0)

Minimal admin-only dashboard to view snapshot outputs and data coverage.

## Dev

```bash
npm install
npm run dev
```

Set backend URL (optional):

```bash
export VITE_API_BASE_URL="http://localhost:8000"
```

